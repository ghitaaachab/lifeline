// Assuming you have a DOM element with id "symptom_<%= @symptom.id %>"
var deletedElement = document.getElementById("symptom_<%= @symptom.id %>");

// Check if the element exists before attempting to remove it
if (deletedElement) {
  deletedElement.parentNode.removeChild(deletedElement);
}
